const { contactSchema, joiContactSchema } = require("./contactSchema");
const { userSchema, userJoiSchema } = require("./userSchema");

module.exports = {
  contactSchema,
  joiContactSchema,
  userSchema,
  userJoiSchema,
};
